import { Canvas } from "@react-three/fiber"; import { Stars } from "@react-three/drei"; import Galaxy from "./Galaxy"; export default function Scene() { return ( <Canvas camera={{ position: [0, 1.5, 20], fov: 55, near: 0.1, far: 1000, }} gl={{ antialias: true, alpha: true, }} dpr={[1, 2]} > {/* Deep Space Background */} <color attach="background" args={["#020617"]} /> {/* Ambient Light */} <ambientLight intensity={1.8} /> {/* Background Stars */} <Stars radius={400} depth={200} count={18000} factor={7} saturation={0} fade speed={1} /> {/* Galaxy */} <Galaxy /> {/* Extra Far Stars */} <Stars radius={700} depth={500} count={5000} factor={12} fade speed={0.2} /> </Canvas> ); }